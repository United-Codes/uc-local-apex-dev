---
title: Backups
description: Learn how to create and manage backups in your local APEX development environment.
sidebar:
  order: 4
---

import { Aside } from "@astrojs/starlight/components";

### Backup All Users

Create datapump dumps of all users, with their APEX workspaces and applications:

```bash
local-26ai.sh backup-all
```

The script writes the files to the `./backups/export` directory.

### Backup Specific Schema

Back up a single schema with its workspace and applications:

```bash
local-26ai.sh backup-user movies
```

### Import Backup

Restore a previously created backup:

```bash
local-26ai.sh import-backup movies
```

<Aside type="note" title="Future Enhancements">
  This command imports the datapump dump. If the user does not exist, the
  command creates it first. I plan to also import APEX workspaces and
  applications later.
</Aside>
